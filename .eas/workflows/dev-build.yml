name: Build

on:
  push:
    branches: ['dev']

jobs:
  build_android:
    name: Build Android App
    type: build
    params:
      platform: android
      profile: development
  build_ios:
    name: Build iOS App
    type: build
    params:
      platform: ios
      profile: development